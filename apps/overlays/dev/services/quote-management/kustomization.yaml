apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../base/dotnet-service

namePrefix: ecommerceddd-quote-management-

commonLabels:
  app.kubernetes.io/name: ecommerceddd-quote-management
  app.kubernetes.io/instance: quote-management

patchesStrategicMerge:
  - patch.yaml
