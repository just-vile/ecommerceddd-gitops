apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../base/dotnet-service

namePrefix: ecommerceddd-signalr-

commonLabels:
  app.kubernetes.io/name: ecommerceddd-signalr
  app.kubernetes.io/instance: signalr

patchesStrategicMerge:
  - patch.yaml
