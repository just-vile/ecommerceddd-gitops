apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../base/dotnet-service

namePrefix: ecommerceddd-inventory-management-

commonLabels:
  app.kubernetes.io/name: ecommerceddd-inventory-management
  app.kubernetes.io/instance: inventory-management

patchesStrategicMerge:
  - patch.yaml
