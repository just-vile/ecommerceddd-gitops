apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: data

resources:
  - kafka-node-pool.yaml   # KafkaNodePool required when strimzi.io/node-pools: enabled (KRaft mode)
  - kafka.yaml
