---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: aspire-dashboard-dev
  namespace: platform

bases:
  - ../../base
  - ingress.yaml

# Patches for dev environment (if needed in future)
# patchesStrategicMerge:
#   - patches.yaml

# Replicas override (optional)
replicas:
  - name: aspire-dashboard
    count: 1
